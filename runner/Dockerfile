FROM python:3.12-slim

ENV PYTHONDONTWRITEBYTECODE=1 \
    PYTHONUNBUFFERED=1 \
    PIP_DISABLE_PIP_VERSION_CHECK=1 \
    PIP_NO_CACHE_DIR=1 \
    DEBIAN_FRONTEND=noninteractive

RUN apt-get update \
 && apt-get install -y --no-install-recommends \
    fio nvme-cli smartmontools hdparm pciutils util-linux jq curl ca-certificates \
 && rm -rf /var/lib/apt/lists/*

WORKDIR /app

COPY runner ./runner
RUN pip install --upgrade pip && pip install -e ./runner
WORKDIR /app/runner

CMD ["anvil-runner", "--socket", "/run/anvil/runner.sock"]
